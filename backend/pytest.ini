[pytest]
testpaths = tests
norecursedirs = tests/integration
markers =
    slow: marks tests that run the full pipeline (deselect with -m "not slow")
    integration: marks tests that require Redis + running Celery worker (deselect with -m "not integration")
